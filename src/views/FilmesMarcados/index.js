import { StyleSheet } from "react-native";
import Tela from "../../components/Tela";

export default function FilmesMarcados({ navigation }) {

    return <Tela>

    </Tela>
}

const estilosTelaFilmesMarcados = StyleSheet.create({
    
});