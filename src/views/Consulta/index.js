import { StyleSheet } from "react-native";
import Tela from "../../components/Tela";

export default function Consulta({ navigation }) {

    return <Tela>

    </Tela>
}

const estilosTelaConsulta = StyleSheet.create({
    
});